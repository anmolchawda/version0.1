
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow any authenticated user to read and write to the database.
    // This is a common setup for development.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
