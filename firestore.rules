
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // For development, allow any authenticated user to read and write to the database.
    // For production, you should tighten these rules to be more secure.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
