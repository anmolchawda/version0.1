
// src/components/layout/app-header.tsx
// This component is effectively no longer used for primary app layout purposes as TopHeader takes over.
// Keeping it empty or minimal to avoid build errors if imported elsewhere by mistake.

export function AppHeader() {
  return null;
}
