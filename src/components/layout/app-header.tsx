// src/components/layout/app-header.tsx
// This component is effectively no longer used in the (app) layout
// as the sidebar is self-contained and a minimal messages icon is handled in AppPagesLayout.
// Keeping it empty to avoid build errors if it's imported elsewhere by mistake.

export function AppHeader() {
  return null;
}
