
// src/components/layout/app-header.tsx
// This component is no longer the primary navigation header.
// It can be removed or repurposed if a top bar is still needed for other elements.
// For now, it will be empty to avoid conflicts.

export function AppHeader() {
  return null; 
}
