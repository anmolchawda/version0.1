// src/components/auth/native-auth-handler.tsx

/**
 * This component is intentionally left empty.
 * The authentication logic is now fully handled by AuthProvider and the native MainActivity.
 * This file is kept to prevent import errors if it's referenced elsewhere,
 * but it performs no actions. It is recommended to delete this file and
 * remove its usage from your layout.
 */
export function NativeAuthHandler() {
  // This component does not render anything and has no logic.
  return null;
}
